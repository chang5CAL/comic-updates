var express = require('express');
var router = express.Router();

var Models = require('../models');

/* Simple find */
router.get('/crawlAll', function(req, res, next) {
	
});

module.exports = router;
